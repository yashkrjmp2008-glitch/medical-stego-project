# Data

Place optional input images here for experiments.

The demo does not require external image files; it generates synthetic
grayscale images automatically when `--cover` and `--watermark` are omitted.

Do not commit confidential or identifiable medical images to a public repository.
